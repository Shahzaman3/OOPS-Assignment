#include <iostream>
using namespace std;

int main(){
      char ch;
      int ascii;
      cout << "Enter a character: ";
      cin >> ch;
      ascii = (int)ch;

      cout << "The character in ASCII is: " << ascii;
      return 0;
}